INSERT INTO biographies (id, name)
VALUES
    ( 001, "Diary of Anne Frank"),
    ( 002, "Frida: A Biography of Frida Kahlo"),
    ( 003, "Long Walk to Freedom");

SELECT * FROM biographies;

// SELECT is used to retrieve rows selected from one or more tables

// We're using insert to insert the following vaules to a
 new rows into an existing table. 
 Then we use "SELECT" to retrive rows 
that we select from the table

